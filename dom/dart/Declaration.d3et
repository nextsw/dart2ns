class Declaration extends Statement {
    DataType type;
    List<String> names = [];
    Expression assignment;
    Boolean isFinal;
    Boolean isConst;
    Boolean isLate;
    Declaration({this.type, this.names, this.assignment, this.isFinal, this.isLate, this.isConst});
}