server class Break extends Statement {
    String label;
    Break(this.label);

    void resolve(ResolveContext context) {

    }
    void collectUsedTypes(List<DataType> types) {
        
    }

    void simplify(Simplifier s){
        
    }
}